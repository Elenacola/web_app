import psycopg2

conn = psycopg2.connect(
    host="168.119.35.175",
    database="auto",
    user="candidato",
    password="crossnection21"
)

cursor = conn.cursor()
cursor.execute("SELECT * FROM auto")
rows = cursor.fetchall()

for row in rows:
    print(row)

conn.close()